{
  "title": "addSubscription_response",
  "description": "An object representing the metadata associated with a push subscription",
  "type": "object",
  "properties": {
    "subscriptions" : {
      "description" : "All existing subscriptions for that user (empty if there aren't any)",
      "type" : "array",
      "items" : {
        "$ref": "../../../common/metadata.schema.json"
      }
    }
  }
}
