{
  "title": "getAllSubscription",
  "description": "An object representing the metadata associated with a push subscription",
  "properties": {
    "subscription" : {
      "$ref": "../../../common/subscription.schema.json"
    }
  },
  "required": ["subscription"]
}
